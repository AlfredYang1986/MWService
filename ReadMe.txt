The edmx file will differ and will have to be changed. 
Also, the App.config and Web.config files shall have to be changed for the connection string to the new Data Entity.


To run tests for Searching Engine with condition>

Copy Brand.xml and color.xml to Desktop first.

Uncomment the test you wish to run in the Main function of Program.cs file and comment every other test.


Thanks.